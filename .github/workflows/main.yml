name : Terraform Checks
on:
  push:

permissions: read-all

jobs:
  terraform:
    runs-on: ubuntu-latest
    permissions: read-all

    steps:
    - name: checkout code
      uses: actions/checkout@v4
  
    - name: setup terraform
      uses: hashicorp/setup-terraform@v1
      with:
        terraform_version: 1.7.3
        terraform_wrapper: false
    
    - name: check format
      run: terraform fmt -recursive -check

    - name: initialize terraform
      run : |
        echo ${{ env.PAT_VAR }}
        echo ${{ vars.PAT_VAR }}
        echo credentials \"github.com\" { >> ~/.terraformrc
        echo    token = \"${{ env.PAT_VAR }}\" >> ~/.terraformrc
        echo } >> ~/.terraformrc
        cat ~/.terraformrc
        #sed -i -e "s/github.com/https:\/\/git:${{ env.PAT_VAR }}@github.com/g" main.tf
        #cat main.tf
        terraform init
        terraform plan
        terraform validate 

  

